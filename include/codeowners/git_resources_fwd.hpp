#pragma once

/// This header contains forward declarations of git-related
/// types provided by libgit2, or wrapper types provided by
/// the codeowners library (as internal implementation
/// details).

struct git_repository;
struct git_index;