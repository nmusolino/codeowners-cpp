language: cpp
compiler:
  - clang
  - gcc
os: osx
osx_image: xcode10.1  # macOS 10.13

install:
    - ./install-deps.sh

script:
    - make all
    - make test_ubsan
    - which git-clang-format || true