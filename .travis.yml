language: cpp
compiler:
  - clang
  - gcc
os:
  - osx
  - linux
dist: trusty
osx_image: xcode10.1
    
install:
    - ./install-deps.sh

script:
    - make all
    - make test_ubsan
    - which git-clang-format || true