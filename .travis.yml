language: cpp
compiler:
  - clang
  - gcc
os:
  - osx
  - linux
dist: trusty
osx_image: xcode10.1

addons:
  apt:
    packages:
      - clang-format
      - cmake      

install:
    - ./install-deps.sh

script:
    - make all
    - echo $(which git-clang-format)