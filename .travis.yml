language: cpp
compiler:
  - clang
  - gcc
os:
  - osx
  - linux
dist: xenial
osx_image: xcode10.1

addons:
  apt:
    packages:
      - clang-format
      - cmake      

matrix:
  allow_failures:
    - os: linux

install:
    - ./install-deps.sh

script:
    - make all
    - echo $(which git-clang-format)